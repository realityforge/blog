h1. This is the data for my blog

Using "rake deploy" it is transformed by "Jekyll":http://github.com/mojombo/jekyll into a static site and rsynced with a remote server.

h4. TODO

* Add in .htaccess that does redirects with appropriate HTTP code for permanent move
* update deployment script to ignore "static" dir
* move static files to new server
* cut across dns to new site
* update deployment script for new server name
* import the blogger stories
* import the babble content
* add post to indicate what is happening with blog
 